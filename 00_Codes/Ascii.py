#To convert an integer to ASCII we use char()
# To convert a character to ASCII we use ord()

String="You are in Anon3605 GitHUB"
Integer=0
for i in String:
    print(str(ord(i)),end="")

for ASCII String we keep the 